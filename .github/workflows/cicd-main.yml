# Copyright (c) 2025, NVIDIA CORPORATION. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
name: CICD NeMo
on:
  schedule:
    - cron: 0 0 * * *
  push:
    branches:
      - main
      - "pull-request/[0-9]+"
      - "deploy-release/*"

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}-${{ github.event.label.name || 'main' }}-${{ github.event_name }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read
  pull-requests: read

env:
  container-registry: ${{ vars.DEFAULT_CONTAINER_REGISTRY }}
  container-registry-gb200: us-east4-docker.pkg.dev/nv-projdgxchipp-20260113193621/automodel

jobs:
  ephemeral-runner-routing:
    uses: NVIDIA-NeMo/FW-CI-templates/.github/workflows/_ephemeral_runner_routing.yml@4fe957d4ea5816af1d67fae130bbab4b434c10b3 # v1.9.2
    with:
      lane: ${{ vars.NEMO_CI_EPHEMERAL_LANE }}
      legacy_label: ${{ vars.NON_NVIDIA_RUNNER_PREFIX }}
      run_id: ${{ github.run_id }}
      run_attempt: ${{ github.run_attempt }}

  pre-flight:
    needs: [ephemeral-runner-routing]
    uses: NVIDIA-NeMo/FW-CI-templates/.github/workflows/_cicd_preflight.yml@d0878a142525ff79366ca96fbc0b306cd8d64670 # v1.9.0
    with:
      default_runner_prefix: ${{ vars.DEFAULT_RUNNER_PREFIX }}
      non_nvidia_runner_prefix: ${{ needs.ephemeral-runner-routing.outputs.runner_label_base }}
      default_test_data_path: ${{ vars.DEFAULT_TEST_DATA_PATH }}
      non_nvidia_test_data_path: ${{ vars.NON_NVIDIA_TEST_DATA_PATH }}
      sso_users_filename: ${{ vars.SSO_USERS_FILENAME }}
    secrets:
      NVIDIA_MANAGEMENT_ORG_PAT: ${{ secrets.NVIDIA_MANAGEMENT_ORG_PAT }}

  linting:
    runs-on: ${{ vars.LINT_AND_INSTALL_TEST_RUNNER || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.10"
      - name: Set up UV
        uses: astral-sh/setup-uv@162b8acf397cb069dec09a3f5a9847cf71cfa46a # v1
        with:
          version: 0.8.22
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /usr/local/lib/node_modules || true
          pip cache purge || true
      - name: Install ruff
        env:
          UV_PROJECT_ENVIRONMENT: ./venv
        run: |
          uv venv ${UV_PROJECT_ENVIRONMENT} --system-site-packages
          source ./venv/bin/activate

          export PATH="./bin/:$PATH"

          uv sync --link-mode copy --locked --group linting
      - name: Run ruff
        run: |
          source ./venv/bin/activate
          uv run --active ruff --version
          uv run --active ruff check --verbose .
          uv run --active ruff format --check --verbose .
      - name: Reject unsafe PyTorch loads
        run: |
          source ./venv/bin/activate
          uv run --active bandit -r app.py nemo_automodel examples scripts tools tutorials -t B614

  import_linting:
    runs-on: ${{ vars.LINT_AND_INSTALL_TEST_RUNNER || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.10"
      - name: Set up UV
        uses: astral-sh/setup-uv@162b8acf397cb069dec09a3f5a9847cf71cfa46a # v1
        with:
          version: 0.8.22
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /usr/local/lib/node_modules || true
          pip cache purge || true
      - name: Install ruff
        env:
          UV_PROJECT_ENVIRONMENT: ./venv
        run: |
          uv venv ${UV_PROJECT_ENVIRONMENT} --system-site-packages
          source ./venv/bin/activate

          export PATH="./bin/:$PATH"

          uv sync --link-mode copy --locked --group linting

      - name: Run import-linter
        run: |
          source ./venv/bin/activate
          uv run lint-imports --debug --verbose --no-cache

  type_checking:
    runs-on: ${{ vars.LINT_AND_INSTALL_TEST_RUNNER || 'ubuntu-latest' }}
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: "3.10"
      - name: Set up UV
        uses: astral-sh/setup-uv@162b8acf397cb069dec09a3f5a9847cf71cfa46a # v1
        with:
          version: 0.8.22
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /usr/local/.ghcup || true
          sudo rm -rf /usr/local/lib/node_modules || true
          pip cache purge || true
      - name: Install ty
        env:
          UV_PROJECT_ENVIRONMENT: ./venv
        run: |
          uv venv ${UV_PROJECT_ENVIRONMENT} --system-site-packages
          source ./venv/bin/activate

          export PATH="./bin/:$PATH"

          uv sync --link-mode copy --locked --group linting

      - name: Run ty
        run: |
          source ./venv/bin/activate
          uv run --active ty --version

          uv pip install -q tomli
          TY_DIRS=$(python3 -c "
          try:
              import tomllib
          except ModuleNotFoundError:
              import tomli as tomllib
          from pathlib import Path
          data = tomllib.loads(Path('pyproject.toml').read_text())
          dirs = set()
          for override in data.get('tool', {}).get('ty', {}).get('overrides', []):
              for pattern in override.get('include', []):
                  dirs.add(pattern.replace('/**', ''))
          print(' '.join(sorted(dirs)))
          ")

          echo "ty check targets: $TY_DIRS"
          uv run --active ty check --verbose $TY_DIRS

  Nemo_Linting_Test:
    needs: [linting, import_linting, type_checking]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Main
        env:
          LINTING_RESULT: ${{ toJson(needs.linting) }}
          IMPORT_LINTING_RESULT: ${{ toJson(needs.import_linting) }}
          TYPE_CHECKING_RESULT: ${{ toJson(needs.type_checking) }}
        run: |
          LINTING=$(echo "$LINTING_RESULT" | jq -r '.result')
          IMPORT_LINTING=$(echo "$IMPORT_LINTING_RESULT" | jq -r '.result')
          TYPE_CHECKING=$(echo "$TYPE_CHECKING_RESULT" | jq -r '.result')

          if [[ "$LINTING" == "success" ]] && [[ "$IMPORT_LINTING" == "success" ]] && [[ "$TYPE_CHECKING" == "success" ]]; then
            echo "All linting checks passed."
            exit 0
          else
            echo "Some linting checks failed:"
            echo "  - linting: $LINTING"
            echo "  - import_linting: $IMPORT_LINTING"
            echo "  - type_checking: $TYPE_CHECKING"
            exit 1
          fi

  cicd-wait-in-queue:
    needs: [pre-flight]
    runs-on: ubuntu-latest
    environment: test
    if: |
      !(needs.pre-flight.outputs.is_ci_workload == 'true'
      || needs.pre-flight.outputs.is_deployment_workflow == 'true'
      || needs.pre-flight.outputs.docs_only == 'true')
    steps:
      - name: Running CI tests
        run: |
          echo "Running CI tests"

  cicd-compute-build-matrix:
    needs: [pre-flight, cicd-wait-in-queue]
    runs-on: ubuntu-latest
    if: |
      (
        success()
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || needs.pre-flight.outputs.force_run_all == 'true'
      )
      && !cancelled()
    outputs:
      matrix: ${{ steps.compute.outputs.matrix }}
    steps:
      - name: Compute build matrix
        id: compute
        env:
          RUNNER_PREFIX: ${{ needs.pre-flight.outputs.runner_prefix }}
          REGISTRY_AWS: ${{ env.container-registry }}
          REGISTRY_GCP: ${{ env.container-registry-gb200 }}
          IS_MEMBER: ${{ needs.pre-flight.outputs.is_member }}
          DISABLE_GB200_TESTS: ${{ vars.DISABLE_GB200_TESTS }}
        run: |
          AWS_ENTRY=$(jq -nc --arg registry "$REGISTRY_AWS" --arg runner "${RUNNER_PREFIX}" \
            '{"cloud": "aws", "registry": $registry, "runner": $runner}')
          if [[ "$IS_MEMBER" == "true" && "$DISABLE_GB200_TESTS" != "true" ]]; then
            GCP_ENTRY=$(jq -nc --arg registry "$REGISTRY_GCP" --arg runner "nemo-ci-gcp-gpu-x2" \
              '{"cloud": "gcp", "registry": $registry, "runner": $runner}')
            MATRIX=$(jq -nc --argjson aws "$AWS_ENTRY" --argjson gcp "$GCP_ENTRY" \
              '{"include": [$aws, $gcp]}')
          else
            MATRIX=$(jq -nc --argjson aws "$AWS_ENTRY" '{"include": [$aws]}')
          fi
          echo "matrix=$MATRIX" | tee -a "$GITHUB_OUTPUT"

  cicd-container-build:
    needs: [pre-flight, cicd-wait-in-queue, cicd-compute-build-matrix]
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.cicd-compute-build-matrix.outputs.matrix) }}
    if: |
      (
        success()
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || needs.pre-flight.outputs.force_run_all == 'true'
      )
      && !cancelled()
    runs-on: ${{ startsWith(matrix.runner, 'ephe-v2-') && format('{0}-a{1}', matrix.runner, github.run_attempt) || matrix.runner }}
    environment: ${{ contains(matrix.registry, 'azure') && 'nemo-ci' || '' }}
    steps:
      - name: Get PR info
        id: get-pr-info
        if: startsWith(github.ref, 'refs/heads/pull-request/')
        uses: nv-gha-runners/get-pr-info@090577647b8ddc4e06e809e264f7881650ecdccf # main

      - name: Get merge commit sha
        shell: bash -x -e -u -o pipefail {0}
        id: sha
        env:
          IS_PR: ${{ startsWith(github.ref, 'refs/heads/pull-request/') }}
        run: |
          if [[ "$IS_PR" == "true" ]]; then
            SHA=${{ fromJSON(steps.get-pr-info.outputs.pr-info || '{}').merge_commit_sha }}
          else
            SHA=${GITHUB_SHA}
          fi
          echo "main=${SHA}" | tee -a "$GITHUB_OUTPUT"

      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ steps.sha.outputs.main }}
          fetch-depth: 0

      - name: Setup python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: 3.12

      - name: Set up Docker Buildx
        id: setup-buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Compute cache keys
        id: cache_keys
        shell: bash
        run: |
          PR_NUMBER="${{ fromJSON(steps.get-pr-info.outputs.pr-info || '{}').number }}"
          BRANCH_SANITIZED=$(echo "${{ github.ref_name }}" | tr '/' '-' | tr -cd '[:alnum:]._-')
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            KEY="main"
          elif [[ -n "$PR_NUMBER" ]]; then
            KEY="$PR_NUMBER"
          else
            KEY="$BRANCH_SANITIZED"
          fi
          echo "key=$KEY" | tee -a "$GITHUB_OUTPUT"
          echo "cache-to=type=registry,ref=${{ matrix.registry }}/automodel:${KEY}-buildcache,mode=max" | tee -a "$GITHUB_OUTPUT"

      # DeepEP and other source builds can produce different result digests for
      # the same BuildKit key. Reading multiple donors can select an incoherent
      # parent chain and invalidate every expensive downstream layer.
      - name: Select cache donor
        id: cache_from
        shell: bash
        env:
          BASELINE_CACHE: ${{ matrix.registry }}/automodel:main-buildcache
          RUN_CACHE: ${{ matrix.registry }}/automodel:${{ steps.cache_keys.outputs.key }}-buildcache
        run: |
          DONOR_CACHE="$RUN_CACHE"
          if ! docker buildx imagetools inspect "$DONOR_CACHE" >/dev/null 2>&1; then
            DONOR_CACHE="$BASELINE_CACHE"
          fi
          echo "donor=$DONOR_CACHE" | tee -a "$GITHUB_OUTPUT"

      - name: Compute platform
        id: platform
        run: |
          if [[ "${{ matrix.cloud }}" == "gcp" ]]; then
            echo "platforms=linux/arm64" | tee -a "$GITHUB_OUTPUT"
            echo "cache-key=arm64" | tee -a "$GITHUB_OUTPUT"
          else
            echo "platforms=linux/amd64" | tee -a "$GITHUB_OUTPUT"
            echo "cache-key=amd64" | tee -a "$GITHUB_OUTPUT"
          fi

      # Keep multi-GB cache ownership on main. Copied PR branches can restore
      # main's cache without consuming repository storage with scoped copies.
      - name: Restore and save uv build cache
        id: uv-build-cache-main
        if: github.ref == 'refs/heads/main'
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: .buildkit-cache/uv
          # Compiled CUDA wheels are ABI-bound to the Dockerfile base image and lockfiles.
          key: buildkit-uv-${{ runner.os }}-${{ steps.platform.outputs.cache-key }}-${{ hashFiles('docker/Dockerfile', 'docker/common/uv-pytorch.lock', 'uv.lock') }}

      - name: Restore uv build cache
        id: uv-build-cache-branch
        if: github.ref != 'refs/heads/main'
        uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
        with:
          path: .buildkit-cache/uv
          # Compiled CUDA wheels are ABI-bound to the Dockerfile base image and lockfiles.
          key: buildkit-uv-${{ runner.os }}-${{ steps.platform.outputs.cache-key }}-${{ hashFiles('docker/Dockerfile', 'docker/common/uv-pytorch.lock', 'uv.lock') }}

      - name: Inject uv cache into BuildKit
        uses: reproducible-containers/buildkit-cache-dance@5b81f4d29dc8397a7d341dba3aeecc7ec54d6361 # v3.3.0
        with:
          builder: ${{ steps.setup-buildx.outputs.name }}
          utility-image: ghcr.io/containerd/busybox@sha256:52f73a0a43a16cf37cd0720c90887ce972fe60ee06a687ee71fb93a7ca601df7
          scratch-dir: .buildkit-cache/scratch
          cache-map: |
            {
              ".buildkit-cache/uv": "/opt/uv_cache"
            }
          # Branch runs are restore-only, so extracting their updated mount
          # after the build would create an archive that no step persists.
          skip-extraction: ${{ github.ref != 'refs/heads/main' || steps.uv-build-cache-main.outputs.cache-hit == 'true' }}

      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.BOT_ID }}
          private-key: ${{ secrets.BOT_KEY }}

      - name: Build and push
        uses: docker/build-push-action@ca052bb54ab0790a636c9b5f226502c73d547a25 # v5
        with:
          file: ./docker/Dockerfile
          push: true
          context: .
          platforms: ${{ steps.platform.outputs.platforms }}
          build-args: |
            BASE_IMAGE=pytorch
          cache-from: type=registry,ref=${{ steps.cache_from.outputs.donor }},mode=max
          cache-to: ${{ steps.cache_keys.outputs.cache-to }}
          no-cache: false
          tags: |
            ${{ matrix.registry }}/automodel:${{ steps.cache_keys.outputs.key }}
            ${{ matrix.registry }}/automodel:${{ github.sha }}
          secrets: |
            GH_TOKEN=${{ steps.app-token.outputs.token }}

  cicd-unit-tests:
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        include:
          - test-name: L0_Unit_Tests_CPU
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            cpu-only: true
            timeout: 15
          - test-name: L0_Unit_Tests_GPU_1_of_2
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            cpu-only: false
            timeout: 30
            shard-id: 0
            num-shards: 2
          - test-name: L0_Unit_Tests_GPU_2_of_2
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            cpu-only: false
            timeout: 30
            shard-id: 1
            num-shards: 2
    needs: [pre-flight, cicd-wait-in-queue, cicd-container-build]
    runs-on: ${{ startsWith(matrix.runner, 'ephe-v2-') && format('{0}-a{1}', matrix.runner, github.run_attempt) || matrix.runner }}
    name: ${{ matrix.test-name }}
    if: |
      (
        success()
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || needs.pre-flight.outputs.force_run_all == 'true'
      )
      && !cancelled()
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.BOT_ID }}
          private-key: ${{ secrets.BOT_KEY }}

      - name: main
        uses: ./.github/actions/test-template
        with:
          timeout: ${{ matrix.timeout || 10 }}
          is-unit-test: "true"
          cpu-only: ${{ matrix.cpu-only || false }}
          shard-id: ${{ matrix.shard-id }}
          num-shards: ${{ matrix.num-shards }}
          PAT: ${{ steps.app-token.outputs.token }}
          test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          container-image: ${{ env.container-registry }}/${{ vars.CI_CONTAINER_NAME || 'automodel' }}:${{ github.sha }}
          runner: ${{ startsWith(matrix.runner, 'ephe-v2-') && format('{0}-a{1}', matrix.runner, github.run_attempt) || matrix.runner }}

  cicd-e2e-tests:
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include:
          - test-name: L2_Pretrain_and_KD
            test-folder: llm_pretrain_and_kd,speculative
            timeout: 30
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_HF_DCP
            test-folder: hf_dcp
            timeout: 40
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_HF_PEFT
            test-folder: hf_peft
            timeout: 30
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          # Merged with hf_transformer_llm + data. The matrix gets `max-parallel: 10`;
          # as 13 separate entries the last three could not start until a wave-1 job
          # freed a runner. These are the three cheapest suites (3.6 + 5.2 + 6.0 =
          # 14.8 min median), well under the 16.6 min L2_HF_Transformer_Finetune that
          # sets the stage floor. A 40-minute timeout leaves more than 2x headroom
          # over the measured merged runtime while bounding timeout retries.
          - test-name: L2_HF_Transformer
            test-folder: hf_transformer,hf_transformer_llm,data
            timeout: 40
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_HF_Transformer_Finetune
            test-folder: hf_transformer_finetune
            timeout: 40
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_HF_Transformer_VLM
            test-folder: hf_transformer_vlm
            timeout: 60
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_Context_Parallel
            test-folder: context_parallel
            timeout: 20
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_Retrieval
            test-folder: retrieval
            timeout: 20
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          - test-name: L2_Diffusion
            test-folder: diffusion,dllm
            timeout: 30
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          # moe + parallelism merged (2.8 + 5.8 = 8.6 min median) to fit `max-parallel: 10`.
          # Each parity test in `parallelism` runs the proxy twice (1-rank baseline +
          # 2-rank parallel), so the budget is roughly double a single smoke test.
          # A 30-minute timeout leaves more than 2x measured-runtime headroom.
          - test-name: L2_Distributed
            test-folder: moe,parallelism
            timeout: 30
            runner: ${{ needs.pre-flight.outputs.runner_prefix }}
            test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
    needs: [pre-flight, cicd-unit-tests]
    runs-on: ${{ startsWith(matrix.runner, 'ephe-v2-') && format('{0}-a{1}', matrix.runner, github.run_attempt) || matrix.runner }}
    name: ${{ matrix.test-name }}
    if: |
      (
        success()
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || needs.pre-flight.outputs.force_run_all == 'true'
      )
      && !cancelled()
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.BOT_ID }}
          private-key: ${{ secrets.BOT_KEY }}

      - name: main
        uses: ./.github/actions/test-template
        with:
          test-folder: ${{ matrix.test-folder }}
          timeout: ${{ matrix.timeout }}
          is_unit_test: "false"
          PAT: ${{ steps.app-token.outputs.token }}
          test-data-path: ${{ matrix.test-data-path }}
          container-image: ${{ env.container-registry }}/${{ vars.CI_CONTAINER_NAME || 'automodel' }}:${{ github.sha }}
          runner: ${{ startsWith(matrix.runner, 'ephe-v2-') && format('{0}-a{1}', matrix.runner, github.run_attempt) || matrix.runner }}

  cicd-e2e-tests-gb200:
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        include:
          - test-name: L2_Pretrain_and_KD
            test-folder: llm_pretrain_and_kd
            timeout: 20
            is-optional: "true"
          - test-name: L2_HF_DCP
            test-folder: hf_dcp
            timeout: 40
            # DeepEP expert-parallel subtest (GPT-OSS MXFP4, ep_size=2) hard-requires
            # NVLink P2P, which on GB200 comes from the IMEX ComputeDomain channel.
            # Route to the imex-lease runner; every other gb200 test stays on the
            # plain (non-IMEX) pool. See nemo-ci-gcp-gpu-x2-imex in TF-States.
            runner: nemo-ci-gcp-gpu-x2-imex
          - test-name: L2_HF_PEFT
            test-folder: hf_peft
            timeout: 30
            is-optional: "true"
          # Merged with hf_transformer_llm + data, mirroring cicd-e2e-tests
          # (2.4 + 3.8 + 4.3 = 10.5 min median, under the 13.2 min L2_Context_Parallel
          # floor). Leaves this matrix at 9 entries, one slot of headroom.
          - test-name: L2_HF_Transformer
            test-folder: hf_transformer,hf_transformer_llm,data
            timeout: 40
          - test-name: L2_HF_Transformer_Finetune
            test-folder: hf_transformer_finetune
            timeout: 40
          - test-name: L2_HF_Transformer_VLM
            test-folder: hf_transformer_vlm
            timeout: 60
            is-optional: "true"
          - test-name: L2_Context_Parallel
            test-folder: context_parallel
            timeout: 20
          - test-name: L2_Retrieval
            test-folder: retrieval
            timeout: 20
          # moe + parallelism merged (2.1 + 4.1 = 6.2 min median), mirroring cicd-e2e-tests.
          - test-name: L2_Distributed
            test-folder: moe,parallelism
            timeout: 30
    needs: [pre-flight, cicd-unit-tests, cicd-container-build]
    runs-on: ${{ startsWith(matrix.runner || 'nemo-ci-gcp-gpu-x2', 'ephe-v2-') && format('{0}-a{1}', matrix.runner || 'nemo-ci-gcp-gpu-x2', github.run_attempt) || matrix.runner || 'nemo-ci-gcp-gpu-x2' }}
    name: gb200_${{ matrix.test-name }}
    if: |
      (
        success()
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || needs.pre-flight.outputs.force_run_all == 'true'
      )
      && !cancelled()
      && needs.pre-flight.outputs.is_member == 'true'
      && vars.DISABLE_GB200_TESTS != 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.BOT_ID }}
          private-key: ${{ secrets.BOT_KEY }}

      - name: main
        uses: ./.github/actions/test-template
        with:
          test-folder: ${{ matrix.test-folder }}
          timeout: ${{ matrix.timeout }}
          is-optional: ${{ matrix.is-optional || 'false' }}
          is_unit_test: "false"
          PAT: ${{ steps.app-token.outputs.token }}
          test-data-path: ${{ needs.pre-flight.outputs.test_data_path }}
          container-image: ${{ env.container-registry-gb200 }}/automodel:${{ github.sha }}
          runner: ${{ startsWith(matrix.runner || 'nemo-ci-gcp-gpu-x2', 'ephe-v2-') && format('{0}-a{1}', matrix.runner || 'nemo-ci-gcp-gpu-x2', github.run_attempt) || matrix.runner || 'nemo-ci-gcp-gpu-x2' }}

  Nemo_CICD_Test:
    needs:
      - pre-flight
      - cicd-unit-tests
      - cicd-e2e-tests
      - cicd-e2e-tests-gb200
    if: |
      (
        needs.pre-flight.outputs.docs_only == 'true'
        || needs.pre-flight.outputs.is_deployment_workflow == 'true'
        || needs.pre-flight.outputs.is_ci_workload == 'true'
        || always()
      )
      && !cancelled()
    runs-on: ubuntu-latest
    permissions: write-all
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Get workflow result
        id: result
        shell: bash -x -e -u -o pipefail {0}
        env:
          GH_TOKEN: ${{ github.token }}
          SKIPPING_IS_ALLOWED: ${{ needs.pre-flight.outputs.docs_only == 'true' || needs.pre-flight.outputs.is_deployment_workflow == 'true' || needs.pre-flight.outputs.is_ci_workload == 'true' }}
          IS_MEMBER: ${{ needs.pre-flight.outputs.is_member }}
          DISABLE_GB200_TESTS: ${{ vars.DISABLE_GB200_TESTS }}
        run: |
          if [[ "$IS_MEMBER" == "true" && "$DISABLE_GB200_TESTS" != "true" ]]; then
            FAILED_JOBS=$(gh run view $GITHUB_RUN_ID --json jobs --jq '[.jobs[] | select(.status == "completed" and .conclusion != "success")] | length') || echo 0
          else
            FAILED_JOBS=$(gh run view $GITHUB_RUN_ID --json jobs --jq '[.jobs[] | select(.status == "completed" and .conclusion != "success" and (.name | startswith("gb200_") | not))] | length') || echo 0
          fi

          if [ "${FAILED_JOBS:-0}" -eq 0 ] || [ "$SKIPPING_IS_ALLOWED" == "true" ]; then
              echo "✅ All previous jobs completed successfully"
              exit 0
          else
              echo "❌ Found $FAILED_JOBS failed job(s)"
              # Show which jobs failed
              gh run view $GITHUB_RUN_ID --json jobs --jq '.jobs[] | select(.status == "completed" and .conclusion != "success") | .name'
              exit 1
          fi

  Coverage_Fake:
    runs-on: ubuntu-latest
    needs: [Nemo_CICD_Test, pre-flight]
    if: |
      (
        needs.pre-flight.outputs.docs_only == 'true'
        || needs.pre-flight.outputs.is_deployment_workflow == 'true'
      )
      && needs.pre-flight.outputs.is_ci_workload == 'false'
      && !cancelled()
      && vars.ENABLE_CODECOV == 'true'
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.BOT_ID }}
          private-key: ${{ secrets.BOT_KEY }}

      - name: Generate fake coverage report
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          github-token: ${{ steps.app-token.outputs.token }}
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: 'success',
              description: 'No code changes - coverage check skipped',
              context: 'codecov/patch'
            });

  Coverage:
    runs-on: ubuntu-latest
    needs: [pre-flight, Nemo_CICD_Test]
    if: |
      !cancelled()
      && needs.Nemo_CICD_Test.result == 'success'
      && vars.ENABLE_CODECOV == 'true'
    strategy:
      matrix:
        flag: [unit-test, e2e]
    steps:
      - name: Get PR info
        id: get-pr-info
        if: startsWith(github.ref, 'refs/heads/pull-request/')
        uses: nv-gha-runners/get-pr-info@090577647b8ddc4e06e809e264f7881650ecdccf # main

      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6

      - name: Download coverage reports of current branch
        uses: actions/download-artifact@37930b1c2abaa49bbe596cd826c3c89aef350131 # v7
        with:
          pattern: coverage-${{ matrix.flag }}-*
          path: coverage-downloads

      - name: Get total coverage of current branch
        shell: bash -x -e -u -o pipefail {0}
        if: always()
        run: |
          pip install coverage[toml]

          if ! [ -d coverage-downloads ] || [ -z "$(ls -A coverage-downloads)" ]; then
            echo "No coverage artifacts found for ${{ matrix.flag }}, skipping."
            exit 0
          fi
          ls -al coverage-downloads/
          coverage combine --keep $(find coverage-downloads -name '.coverage')
          coverage report -i
          coverage xml -i
          rm -rf coverage-downloads
          ls -al

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@0fb7174895f61a3b6b78fc075e0cd60383518dac # v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true
          flags: ${{ matrix.flag }}
          base_sha: ${{ fromJSON(steps.get-pr-info.outputs.pr-info || '{}').base.sha }}
          override_pr: ${{ fromJSON(steps.get-pr-info.outputs.pr-info || '{}').number }}
          override_commit: ${{ fromJSON(steps.get-pr-info.outputs.pr-info || '{}').head.sha }}

      - name: Upload artifacts
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: coverage-${{ matrix.flag }}-aggregated
          path: |
            .coverage
          include-hidden-files: true
